# activerb

Replication of activation verbalization research — probing and interpreting internal representations of language models via natural language.

## Setup

```bash
uv sync
```

## Development

```bash
uv run pytest
uv run ruff check .
uv run ruff format .
```
